export type CompanyDataType = {
    imgSrc: string;
}

export type MintButtonProps = {
    amount: number;
  }

export type NavigationItem = {
    name: string;
    href: string;
    current: boolean;
}
